spring:
  application:
    name: config-client
  cloud:
    config:
      uri: http://192.168.50.5:8769
      fail-fast: true
    bus:
      enabled: true
      trace:
        enabled: true
  rabbitmq:
    host: 192.168.50.184
    port: 5672
    username: admin
    password: admin
management:
  endpoint:
    bus-refresh:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
server:
  port: 8762
