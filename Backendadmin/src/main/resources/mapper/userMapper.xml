<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.knowimage.mapper.UserMapper">
    <select id="findUserNameById" parameterType="java.lang.String" resultType="java.lang.String">
        select username from user where id = #{id} limit 1
    </select>

    <select id="findIdByUserName" parameterType="java.lang.String" resultType="java.lang.String">
        select id from user where username = #{username} limit 1
    </select>

    <select id="findAllUserNameAndId" resultType="cn.knowimage.pojo.User">
        select username,id from user
    </select>

</mapper>