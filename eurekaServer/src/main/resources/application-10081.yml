spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: eurekaServer
  boot:
    admin:
      client:
        url: http://192.168.50.107:10001
  cloud:
    client:
      ip-address: 192.168.50.154

eureka:
  instance:
    prefer-ip-address: true
    hostname: EurekaServer1
    instance-id: ${spring.cloud.client.ip-address}:${spring.application.name}:${server.port}
    ip-address: ${spring.cloud.client.ip-address}
  client:
    service-url:
      defaultZone: http://192.168.50.154:10082/eureka,http://192.168.50.184:10084/eureka
    register-with-eureka: true
    fetch-registry: true




server:
  port: 10081


management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true

